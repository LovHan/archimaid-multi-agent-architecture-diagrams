# LLM 配置（支持任何 OpenAI 兼容接口）
OPENAI_API_KEY=sk-xxx
OPENAI_BASE_URL=https://api.openai.com/v1
PLANNER_MODEL=gpt-4o
CRITIC_MODEL=gpt-4o
VLM_MODEL=gpt-4o

# draw.io headless export（可选，不配则跳过 PNG 渲染）
DRAWIO_DOCKER_IMAGE=rlespinasse/drawio-desktop-headless:latest
